export enum Roles {
  STUDENT = "ROLE_STUDENT",
  TEACHER = "ROLE_TEACHER",
  ADMIN = "ROLE_ADMIN",
}
export enum TopicStatus {
  Pending = "pending",
  RequestChange = "requestChange",
  Accepted = "accepted",
}